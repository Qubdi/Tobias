docker compose down -v   # remove volume + container
docker compose up -d     # restart clean with init.sql


# if first time doesn work
docker compose down -v --remove-orphans
docker compose up -d     # restart clean with init.sql

# if second time desnt wokk
docker compose down -v --remove-orphans
docker compose build --no-cache
docker compose up -d
